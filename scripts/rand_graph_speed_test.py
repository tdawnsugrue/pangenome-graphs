import subprocess
import os

gdir = "graphs/random"
graphs = os.listdir(gdir)

# have thing for data [ID] [TIME]

# for each graph:
    # pipeline = [whatever]
    # I'm gonna do something real ugly

# do like
# subprocess.run(
#   ["time"])

# HI TOBY (It's me from the past)
# Push gen_random and get_max FROM aoraki before pulling (might need to stash)
# and then push from local